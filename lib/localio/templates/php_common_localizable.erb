<!--
lang-common.php
YOUR_COMPANY

GENERATED - DO NOT MODIFY - use localizable-generator instead.

Created by localizable-generator on <%= Time.now.localtime %>.
Copyright (c) <%= Time.now.year %> YOUR_COMPANY. All rights reserved.
-->

<?php
session_start();
header('Cache-control: private'); // IE 6 FIX

if(isSet($_GET['lang']))
{
	$lang = $_GET['lang'];

	// register the session and set the cookie
	$_SESSION['lang'] = $lang;

	setcookie('lang', $lang, time() + (3600 * 24 * 30));
} else if(isSet($_SESSION['lang'])) {
	$lang = $_SESSION['lang'];
} else if(isSet($_COOKIE['lang'])) {
	$lang = $_COOKIE['lang'];
} else {
	$lang = '<%= @default_language %>';
}

switch ($lang) {
	<% @langs.each do |lang| %>
  case '<%= lang %>':
  $lang_file = 'lang.<%= lang %>.php';
  break;	
	<% end%>

  default:
  $lang_file = 'lang.<%= @default_language %>.php';

}

include_once $lang_file;
?>